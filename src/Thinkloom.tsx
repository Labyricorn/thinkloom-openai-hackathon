"use client";
import { forwardRef, useCallback, useEffect, useImperativeHandle, useRef, useState } from "react";
import { EditorContent, useEditor } from "@tiptap/react";
import StarterKit from "@tiptap/starter-kit";
import { Markdown } from "@tiptap/markdown";
import ProvenanceWorkspace from "./ProvenanceWorkspace";
import HarpExportPanel from "./HarpExportPanel";

type Phase = "ideation" | "drafting" | "finalization";
type View = Phase | "project" | "history" | "provenance" | "export" | "settings";
type IdeaStatus = "suggested" | "accepted" | "rejected" | "archived";
type Actor = "user" | "assistant" | "system";
type Persona = "Supportive Coach" | "Critical Editor" | "Creative Partner";
interface Turn { id: string; speaker: "user" | "assistant"; text: string; createdAt: string; inputMode?: "typed" | "voice_transcription"; invocationId?: string }
interface SessionSnapshot { id: string; title: string; createdAt: string; updatedAt: string; persona: Persona; challenge: State["challenge"]; genre: string; lore: string; turns: Turn[]; workspace: string; summary: string }
interface Idea { id: string; title: string; summary: string; detail: string; status: IdeaStatus; sourceTurnIds: string[]; parentIdeaIds: string[]; tags: string[]; pinned: boolean; selected: boolean; group?: string; createdBy: "user" | "assistant" | "mixed"; revisionNumber?: number; createdAt?: string; updatedAt?: string }
interface Activity { id: string; type: string; actor: Actor; at: string; summary: string; provider?: string }
interface Checkpoint { id: string; name: string; at: string; manuscript: string; words: number }
interface Provider { kind: "ollama" | "openai" | "compatible"; name: string; endpoint: string; model: string; mode: "local" | "cloud"; connected: boolean }
interface State { id: string; title: string; subtitle: string; phase: Phase; privacy: "Local" | "Cloud" | "Mixed"; cloudApproved: boolean; challenge: "Gentle" | "Balanced" | "Rigorous"; spokenReplies: boolean; persona: Persona; genre: string; lore: string; workspace: string; summary: string; activeSessionId: string; sessionTitle: string; sessionCreatedAt: string; sessions: SessionSnapshot[]; turns: Turn[]; ideas: Idea[]; manuscript: string; generation: { id: string; state: "idle" | "streaming" | "staged" | "accepted" | "rejected" | "failed"; text: string; prompt: string; relation: string }; activity: Activity[]; checkpoints: Checkpoint[]; provider: Provider; styleTraits: string[]; disallowedHabits: string[]; finalized: boolean; updatedAt: string }
interface Phase1Operation { kind: string; [key: string]: unknown }
interface Phase1Projection { projectId: string; activeSessionId: string; sessionTitle: string; sessionCreatedAt: string; sessions: SessionSnapshot[]; turns: Turn[]; ideas: Idea[]; persona: Persona; challenge: State["challenge"]; genre: string; lore: string; workspace: string; summary: string; provider: Provider; cloudApproved: boolean; activity: Activity[]; updatedAt: string }
interface Phase1CommandResult { projection: Phase1Projection }
type CompositionOrigin = "recorded_direct_human_input" | "human_expressive_input_via_transcription" | "accepted_ai_output" | "imported_or_pasted" | "system_restoration" | "unattested";
type CompositionBoundary = "idle" | "focus_loss" | "section_change" | "ai_operation" | "checkpoint" | "phase_change" | "explicit_save" | "document_close" | "paste" | "restoration";
type CompositionOperationKind = "insert" | "delete" | "replace" | "move" | "paste" | "transcription" | "ai_acceptance" | "restoration";
interface ScalarRange { start: number; end: number }
interface AiAcceptanceContext { invocationId: string; responseText: string; acceptedRanges: ScalarRange[]; rejectedRanges: ScalarRange[]; partial: boolean }
interface CompositionSignal { boundary: CompositionBoundary; origin: CompositionOrigin; operationKindHint?: CompositionOperationKind; aiAcceptance?: AiAcceptanceContext }
interface CompositionProjection { initialized: boolean; manuscript: string; revisionId: string; operationCount: number; spans: Array<{ segmentId: string; start: number; end: number; origin: CompositionOrigin; lineageReferenceIds: string[] }>; updatedAt: string }
interface CompositionCommandResult { projection: CompositionProjection }

const now = () => new Date().toISOString();
const uid = (prefix: string) => `${prefix}_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 7)}`;
const countWords = (value: string) => value.trim() ? value.trim().split(/\s+/).length : 0;
const fmt = (value: string) => new Intl.DateTimeFormat(undefined, { month: "short", day: "numeric", hour: "numeric", minute: "2-digit" }).format(new Date(value));
const clean = (md: string) => md.replace(/^#{1,6}\s+/gm, "").replace(/[*_`>]/g, "");
const escapeHtml = (v: string) => v.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
const toHtml = (md: string) => md.split("\n").map((line) => line.startsWith("# ") ? `<h1>${escapeHtml(line.slice(2))}</h1>` : line.startsWith("## ") ? `<h2>${escapeHtml(line.slice(3))}</h2>` : line.trim() ? `<p>${escapeHtml(line)}</p>` : "").join("\n");
const personaInstructions: Record<Persona, string> = {
  "Supportive Coach": "Be an encouraging writing coach. Ask clarifying questions and help the writer expand promising ideas without taking control of them.",
  "Critical Editor": "Be a rigorous critical editor. Focus on structure, logic, and clarity; respectfully identify contradictions, weak assumptions, and plot holes.",
  "Creative Partner": "Be an inventive creative partner. Explore what-if scenarios, world-building possibilities, surprising connections, and unexpected but relevant turns.",
};
const genreInstructions: Record<string, string> = {
  Fiction: "Support fiction development with attention to character, conflict, setting, causality, and narrative momentum.",
  "Non-Fiction": "Support non-fiction development with attention to thesis, evidence, reader value, structure, and factual precision.",
  Academic: "Support academic development with attention to research questions, argument structure, evidence, counterarguments, and precise claims.",
  Poetry: "Support poetry development with attention to image, sound, compression, line, voice, and emotional resonance.",
  Screenplay: "Support screenplay development with attention to scene objectives, visual action, dialogue, pacing, and dramatic turns.",
};
const voiceHints: Record<Persona, string[]> = { "Supportive Coach": ["Samantha", "Jenny", "Zira"], "Critical Editor": ["Daniel", "David", "Mark"], "Creative Partner": ["Ava", "Aria", "Sonia"] };

const initial: State = {
  id: "project_attention_commons", title: "The Attention Commons", subtitle: "An essay on reclaiming shared focus", phase: "ideation", privacy: "Local", cloudApproved: false, challenge: "Balanced", spokenReplies: false, persona: "Supportive Coach", genre: "Non-Fiction", lore: "A long-form essay exploring attention as a shared civic condition rather than only a matter of personal discipline.", workspace: "Attention may be a form of civic infrastructure.\n\nInstitutions shape whether sustained focus is possible.\n\nPersonal digital-minimalism advice cannot repair systems designed around extraction.", summary: "Attention should be framed as civic infrastructure: a shared condition shaped by institutional norms, incentives, architecture, and technology. The essay will contrast individual self-management with systemic responsibility, using classrooms, meetings, and public debate as examples of spaces where degraded attention creates collective costs.", activeSessionId: "session_1", sessionTitle: "Attention as civic infrastructure", sessionCreatedAt: "2026-07-16T17:42:00.000Z", sessions: [],
  turns: [
    { id: "turn_1", speaker: "user", text: "I keep returning to the idea that attention is treated as a private resource, even though its erosion changes public life.", createdAt: "2026-07-16T17:42:00.000Z" },
    { id: "turn_2", speaker: "assistant", text: "That tension feels central: we experience distraction individually, but the consequences accumulate socially. Where do you see that shared cost most clearly?", createdAt: "2026-07-16T17:43:00.000Z" },
    { id: "turn_3", speaker: "user", text: "In classrooms, meetings, and even public debate. We have fewer places where sustained attention is expected and protected.", createdAt: "2026-07-16T17:44:00.000Z" },
  ],
  ideas: [
    { id: "idea_1", title: "Attention as civic infrastructure", summary: "Treat sustained attention like a shared condition that institutions can protect or degrade.", detail: "Move beyond individual self-control and ask what healthy attention-supporting institutions look like.", status: "accepted", sourceTurnIds: ["turn_1", "turn_3"], parentIdeaIds: [], tags: ["thesis", "public life"], pinned: true, selected: true, group: "Core argument", createdBy: "mixed" },
    { id: "idea_2", title: "The vanishing expectation", summary: "Public spaces increasingly assume interruption instead of sustained presence.", detail: "Contrast spaces across expectations rather than generations.", status: "suggested", sourceTurnIds: ["turn_3"], parentIdeaIds: [], tags: ["observation"], pinned: false, selected: false, createdBy: "assistant" },
    { id: "idea_3", title: "Beyond digital minimalism", summary: "Personal habits matter, but they cannot repair systems designed around extraction.", detail: "A counterpoint to purely individual advice about focus.", status: "suggested", sourceTurnIds: ["turn_1"], parentIdeaIds: [], tags: ["counterpoint"], pinned: false, selected: false, createdBy: "assistant" },
  ],
  manuscript: "# The Attention Commons\n\nWe have learned to talk about attention as if it were a private possession. We protect it with timers, muted notifications, and carefully chosen routines. Yet the places where attention matters most—classrooms, meetings, courts, and public debate—are shared. When sustained focus becomes rare, the loss does not remain private.\n\n## A public condition\n\nAttention is not simply something an individual brings into a room. It is also something the room can make possible. Norms, incentives, architecture, and technology all determine whether concentration is supported or quietly auctioned away.\n",
  generation: { id: "", state: "idle", text: "", prompt: "", relation: "synthesize" },
  activity: [
    { id: "activity_1", type: "PROJECT_CREATED", actor: "user", at: "2026-07-16T17:42:00.000Z", summary: "Created The Attention Commons" },
    { id: "activity_2", type: "IDEA_ACCEPTED", actor: "user", at: "2026-07-16T17:45:00.000Z", summary: "Accepted “Attention as civic infrastructure”" },
  ],
  checkpoints: [{ id: "version_1", name: "Opening premise", at: "2026-07-16T17:50:00.000Z", manuscript: "# The Attention Commons\n\nWe have learned to talk about attention as if it were a private possession.", words: 14 }],
  provider: { kind: "ollama", name: "Ollama", endpoint: "http://127.0.0.1:11434", model: "llama3.2", mode: "local", connected: false },
  styleTraits: ["Clear, reflective sentences", "Concrete institutional examples", "Measured, invitational argument"], disallowedHabits: ["Overstated certainty", "Generic scene-setting"], finalized: false, updatedAt: "2026-07-16T17:42:00.000Z",
};

function normalizeState(value: Partial<State> & { events?: Activity[] }): State {
  return { ...initial, ...value, activity: Array.isArray(value.activity) ? value.activity : Array.isArray(value.events) ? value.events.map(({ id, type, actor, at, summary, provider }) => ({ id, type, actor, at, summary, provider })) : [], persona: value.persona ?? initial.persona, genre: value.genre ?? initial.genre, lore: value.lore ?? "", workspace: value.workspace ?? "", summary: value.summary ?? "", activeSessionId: value.activeSessionId ?? uid("session"), sessionTitle: value.sessionTitle ?? "Recovered ideation session", sessionCreatedAt: value.sessionCreatedAt ?? value.updatedAt ?? now(), sessions: Array.isArray(value.sessions) ? value.sessions : [], turns: Array.isArray(value.turns) ? value.turns : [] };
}
function createEmptyProject(id = uid("project")): State {
  const createdAt = now();
  const title = "Untitled Project";
  const summary = `Created ${title}`;
  return {
    id, title, subtitle: "", phase: "ideation", privacy: "Local", cloudApproved: false, challenge: "Balanced", spokenReplies: false, persona: "Supportive Coach", genre: "Fiction", lore: "", workspace: "", summary: "", activeSessionId: uid("session"), sessionTitle: "New ideation session", sessionCreatedAt: createdAt, sessions: [],
    turns: [], ideas: [], manuscript: "",
    generation: { id: "", state: "idle", text: "", prompt: "", relation: "synthesize" },
    activity: [{ id: uid("action"), type: "PROJECT_CREATED", actor: "user", at: createdAt, summary }],
    checkpoints: [],
    provider: { kind: "ollama", name: "Ollama", endpoint: "http://127.0.0.1:11434", model: "llama3.2", mode: "local", connected: false },
    styleTraits: [], disallowedHabits: [], finalized: false, updatedAt: createdAt,
  };
}

async function invokeNative<T>(command: string, args: Record<string, unknown> = {}): Promise<T | null> { const host = window as unknown as { __TAURI_INTERNALS__?: { invoke: (name: string, payload: Record<string, unknown>) => Promise<T> } }; return host.__TAURI_INTERNALS__ ? host.__TAURI_INTERNALS__.invoke(command, args) : null; }
function phase1Idea(idea: Idea, changedAt = idea.updatedAt ?? now()): Idea { return { ...idea, revisionNumber: idea.revisionNumber ?? 1, createdAt: idea.createdAt ?? changedAt, updatedAt: changedAt }; }
function phase1Turn(turn: Turn): Turn { return { ...turn, inputMode: turn.inputMode ?? "typed" }; }
function phase1Seed(state: State) { return { activeSessionId: state.activeSessionId, sessionTitle: state.sessionTitle, sessionCreatedAt: state.sessionCreatedAt, sessions: state.sessions.map((session) => ({ ...session, challenge: session.challenge ?? state.challenge, turns: session.turns.map(phase1Turn) })), turns: state.turns.map(phase1Turn), ideas: state.ideas.map((idea) => phase1Idea(idea, idea.updatedAt ?? state.updatedAt)), persona: state.persona, challenge: state.challenge, genre: state.genre, lore: state.lore, workspace: state.workspace, summary: state.summary, provider: state.provider, cloudApproved: state.cloudApproved, updatedAt: state.updatedAt }; }
function mergePhase1Projection(current: State, projection: Phase1Projection): State { return normalizeState({ ...current, id: projection.projectId, activeSessionId: projection.activeSessionId, sessionTitle: projection.sessionTitle, sessionCreatedAt: projection.sessionCreatedAt, sessions: projection.sessions, turns: projection.turns, ideas: projection.ideas, persona: projection.persona, challenge: projection.challenge, genre: projection.genre, lore: projection.lore, workspace: projection.workspace, summary: projection.summary, provider: projection.provider, privacy: projection.provider.mode === "local" ? "Local" : "Cloud", cloudApproved: projection.cloudApproved, activity: projection.activity, updatedAt: projection.updatedAt }); }
async function initializeNativePhase1(state: State, summary: string): Promise<void> { await invokeNative<Phase1CommandResult>("apply_phase1_command", { command: { clientActionId: uid("action"), actor: "user", summary, occurredAt: state.updatedAt, operation: { kind: "INITIALIZE", seed: phase1Seed(state) } } }); }
async function initializeNativeComposition(state: State, origin: CompositionOrigin = "unattested"): Promise<CompositionProjection | null> { const result = await invokeNative<CompositionCommandResult>("apply_composition_command", { command: { clientActionId: uid("action"), actor: "system", summary: "Established the manuscript composition baseline", occurredAt: state.updatedAt, action: { kind: "INITIALIZE", text: state.manuscript, origin } } }); return result?.projection ?? null; }
const scalarLength = (value: string) => Array.from(value).length;
const sectionSignature = (value: string) => value.split("\n").filter((line) => /^#{1,6}\s/.test(line)).join("\n");
async function refreshNativeFiles(appState: State): Promise<void> { await invokeNative("refresh_non_phase1_files", { appState }); }
interface NativeVerificationReport { status: "VERIFIED" | "VERIFIED_WITH_WARNINGS" | "INCOMPLETE" | "FAILED" | "UNSAFE"; event_count: number; record_count: number; findings: Array<{ severity: string; message: string }> }
interface ProjectOpenResult { path: string; editable: boolean; classification: "CPL_CONFORMING" | "LEGACY_PREVIEW_READ_ONLY" | "UNSUPPORTED_READ_ONLY" | "CPL_BLOCKED"; message: string; manifest?: { project_id: string } }
interface ReadOnlyProjectNotice { path: string; classification: ProjectOpenResult["classification"]; message: string }
function nativeError(error: unknown): string { if (error && typeof error === "object") { const value = error as { message?: unknown; code?: unknown }; if (typeof value.message === "string") return value.code ? `${String(value.code)}: ${value.message}` : value.message; } return String(error); }
function download(name: string, contents: string, type = "text/plain") { const href = URL.createObjectURL(new Blob([contents], { type })); const a = document.createElement("a"); a.href = href; a.download = name; a.click(); URL.revokeObjectURL(href); }
function Mark({ children, tone = "neutral" }: { children: React.ReactNode; tone?: string }) { return <span className={`mark mark-${tone}`}>{children}</span>; }
function Empty({ eyebrow, title, body }: { eyebrow: string; title: string; body: string }) { return <div className="empty"><span className="eyebrow">{eyebrow}</span><h2>{title}</h2><p>{body}</p></div>; }

interface StructuredEditorHandle { focus: () => void; insert: (markdown: string, replace: boolean, signal: CompositionSignal) => string; }
const StructuredEditor = forwardRef<StructuredEditorHandle, { value: string; onChange: (value: string, signal?: CompositionSignal) => void; onCommit: (boundary: CompositionBoundary) => void; label?: string }>(function StructuredEditor({ value, onChange, onCommit, label = "Structured manuscript editor" }, ref) {
  const nextSignal = useRef<CompositionSignal | undefined>(undefined);
  const instance = useEditor({
    extensions: [StarterKit, Markdown],
    content: value,
    contentType: "markdown",
    immediatelyRender: false,
    editorProps: { attributes: { class: "tiptap-document", "aria-label": label } },
    onUpdate: ({ editor }) => { const signal = nextSignal.current; nextSignal.current = undefined; onChange(editor.getMarkdown(), signal); },
    onBlur: () => onCommit("focus_loss"),
  });
  useEffect(() => { if (instance && instance.getMarkdown() !== value) instance.commands.setContent(value, { contentType: "markdown", emitUpdate: false }); }, [instance, value]);
  useImperativeHandle(ref, () => ({
    focus: () => { instance?.commands.focus(); },
    insert: (markdown, replace, signal) => { if (!instance) return value; nextSignal.current = signal; if (replace) instance.chain().focus().deleteSelection().insertContent(markdown, { contentType: "markdown" }).run(); else instance.chain().focus().insertContent(markdown, { contentType: "markdown" }).run(); return instance.getMarkdown(); },
  }), [instance, value]);
  if (!instance) return <div className="manuscript-editor editor-loading">Preparing structured editor…</div>;
  return <div className="manuscript-editor structured-editor" onPasteCapture={() => { nextSignal.current = { boundary: "paste", origin: "imported_or_pasted", operationKindHint: "paste" }; }} onDropCapture={() => { nextSignal.current = { boundary: "section_change", origin: "unattested", operationKindHint: "move" }; }}><div className="format-toolbar" aria-label="Text formatting"><button type="button" onClick={() => instance.chain().focus().toggleBold().run()} aria-pressed={instance.isActive("bold")}><strong>B</strong></button><button type="button" onClick={() => instance.chain().focus().toggleItalic().run()} aria-pressed={instance.isActive("italic")}><em>I</em></button><button type="button" onClick={() => instance.chain().focus().toggleHeading({ level: 2 }).run()} aria-pressed={instance.isActive("heading", { level: 2 })}>H2</button><button type="button" onClick={() => instance.chain().focus().toggleBulletList().run()} aria-pressed={instance.isActive("bulletList")}>List</button><button type="button" onClick={() => instance.chain().focus().undo().run()} disabled={!instance.can().undo()}>Undo</button><button type="button" onClick={() => instance.chain().focus().redo().run()} disabled={!instance.can().redo()}>Redo</button></div><EditorContent editor={instance} /></div>;
});
export default function Thinkloom() {
  const [project, setProject] = useState<State>(initial); const [hydrated, setHydrated] = useState(false); const [view, setView] = useState<View>("ideation"); const [message, setMessage] = useState(""); const [messageInputMode, setMessageInputMode] = useState<"typed" | "voice_transcription">("typed"); const [notice, setNotice] = useState("Project ready. History is being recorded."); const [busy, setBusy] = useState(false); const [listening, setListening] = useState(false); const [newHabit, setNewHabit] = useState(""); const [credential, setCredential] = useState(""); const [projectPath, setProjectPath] = useState(""); const [readOnlyProject, setReadOnlyProject] = useState<ReadOnlyProjectNotice | null>(null); const [promptPath, setPromptPath] = useState(""); const editor = useRef<StructuredEditorHandle>(null); const conversationEnd = useRef<HTMLDivElement>(null); const phase1Queue = useRef<Promise<unknown>>(Promise.resolve()); const compositionQueue = useRef<Promise<unknown>>(Promise.resolve()); const committedManuscript = useRef(initial.manuscript); const latestManuscript = useRef(initial.manuscript); const activeProjectPath = useRef(""); const idleCompositionTimer = useRef<number | null>(null);
  useEffect(() => { queueMicrotask(() => { try { const raw = localStorage.getItem("thinkloom-project-v1"); if (raw) { const recovered = normalizeState(JSON.parse(raw) as Partial<State>); committedManuscript.current = recovered.manuscript; latestManuscript.current = recovered.manuscript; setProject(recovered); } } catch { /* Ignore invalid local recovery state. */ } setHydrated(true); }); }, []);
  useEffect(() => { if (hydrated) localStorage.setItem("thinkloom-project-v1", JSON.stringify(project)); }, [project, hydrated]);
  useEffect(() => { void invokeNative<{ directory: string }>("ensure_prompt_files").then((info) => { if (info?.directory) setPromptPath(info.directory); }).catch((error) => setNotice(`Prompt configuration could not be prepared: ${nativeError(error)}`)); }, []);
  useEffect(() => { activeProjectPath.current = projectPath; }, [projectPath]);
  const commitComposition = useCallback((afterText: string, signal: CompositionSignal, summary = "Recorded a manuscript composition transaction"): Promise<boolean> => {
    if (idleCompositionTimer.current !== null) { window.clearTimeout(idleCompositionTimer.current); idleCompositionTimer.current = null; }
    const queued = compositionQueue.current.then(async () => {
      const beforeText = committedManuscript.current;
      if (beforeText === afterText) return true;
      if (!activeProjectPath.current) { committedManuscript.current = afterText; return true; }
      const actor: Actor = signal.origin === "accepted_ai_output" ? "assistant" : signal.origin === "system_restoration" ? "system" : "user";
      const result = await invokeNative<CompositionCommandResult>("apply_composition_command", { command: { clientActionId: uid("action"), actor, summary, occurredAt: now(), action: { kind: "EDIT", beforeText, afterText, boundary: signal.boundary, origin: signal.origin, operationKindHint: signal.operationKindHint, aiAcceptance: signal.aiAcceptance } } });
      committedManuscript.current = result?.projection.manuscript ?? afterText;
      return true;
    });
    compositionQueue.current = queued.catch(() => undefined);
    return queued.catch((error) => { if (latestManuscript.current === afterText) { latestManuscript.current = committedManuscript.current; setProject((current) => ({ ...current, manuscript: committedManuscript.current })); } setNotice(`Composition transaction could not be committed: ${nativeError(error)}`); return false; });
  }, []);
  const handleManuscriptChange = useCallback((manuscript: string, signal?: CompositionSignal) => {
    const previous = latestManuscript.current;
    latestManuscript.current = manuscript;
    setProject((current) => ({ ...current, manuscript, updatedAt: now() }));
    if (idleCompositionTimer.current !== null) window.clearTimeout(idleCompositionTimer.current);
    if (signal) { void commitComposition(manuscript, signal); return; }
    if (sectionSignature(previous) !== sectionSignature(manuscript)) { void commitComposition(manuscript, { boundary: "section_change", origin: "recorded_direct_human_input" }, "Recorded a human-authored section change"); return; }
    idleCompositionTimer.current = window.setTimeout(() => { idleCompositionTimer.current = null; void commitComposition(latestManuscript.current, { boundary: "idle", origin: "recorded_direct_human_input" }, "Recorded a coalesced direct-human edit"); }, 900);
  }, [commitComposition]);
  const flushComposition = useCallback((boundary: CompositionBoundary, origin: CompositionOrigin = "recorded_direct_human_input", operationKindHint?: CompositionOperationKind) => commitComposition(latestManuscript.current, { boundary, origin, operationKindHint }, `Recorded manuscript changes at the ${boundary.replaceAll("_", " ")} boundary`), [commitComposition]);
  useEffect(() => { const close = () => { void flushComposition("document_close"); }; const visibility = () => { if (document.visibilityState === "hidden") close(); }; window.addEventListener("beforeunload", close); document.addEventListener("visibilitychange", visibility); return () => { window.removeEventListener("beforeunload", close); document.removeEventListener("visibilitychange", visibility); }; }, [flushComposition]);
  const mutate = useCallback((type: string, summary: string, update: (current: State) => State, actor: Actor = "user") => { setProject((current) => { const activity: Activity = { id: uid("action"), type, actor, at: now(), summary, provider: actor === "assistant" ? current.provider.name : undefined }; const next = { ...update(current), activity: [...current.activity, activity], updatedAt: activity.at }; void refreshNativeFiles(next).catch(() => undefined); return next; }); setNotice(summary); }, []);
  const commitPhase1 = useCallback(async (operation: Phase1Operation, type: string, summary: string, update: (current: State) => State, actor: Actor = "user"): Promise<boolean> => { const activity: Activity = { id: uid("action"), type, actor, at: now(), summary }; setProject((current) => ({ ...update(current), activity: [...current.activity, { ...activity, provider: actor === "assistant" ? current.provider.name : undefined }], updatedAt: activity.at })); setNotice(summary); try { const committed = phase1Queue.current.then(() => invokeNative<Phase1CommandResult>("apply_phase1_command", { command: { clientActionId: activity.id, actor, summary, occurredAt: activity.at, operation } })); phase1Queue.current = committed.catch(() => undefined); await committed; return true; } catch (error) { setNotice(`Phase 1 action could not be committed: ${nativeError(error)}`); return false; } }, []);
  const navigate = async (next: View) => { if (["ideation", "drafting", "finalization"].includes(next) && project.phase !== next) { await flushComposition("phase_change"); mutate("PHASE_CHANGED", `Moved to ${next}`, (current) => ({ ...current, phase: next as Phase })); } setView(next); };
  useEffect(() => { const shortcut = (e: KeyboardEvent) => { if (!e.altKey) return; if (e.key === "1") document.getElementById("ideas-panel")?.focus(); if (e.key === "2") editor.current?.focus(); if (e.key === "3") document.getElementById("assistant-panel")?.focus(); }; window.addEventListener("keydown", shortcut); return () => window.removeEventListener("keydown", shortcut); }, []);
  const draftingIdeas = project.ideas.filter((idea) => idea.status === "accepted"); const selectedIdeas = draftingIdeas.filter((idea) => idea.selected);
  const snapshotSession = (current: State): SessionSnapshot => ({ id: current.activeSessionId, title: current.sessionTitle, createdAt: current.sessionCreatedAt, updatedAt: current.updatedAt, persona: current.persona, challenge: current.challenge, genre: current.genre, lore: current.lore, turns: current.turns, workspace: current.workspace, summary: current.summary });
  const startNewSession = () => { const createdAt = now(); const sessionId = uid("session"); const title = `Ideation session ${project.sessions.length + 2}`; commitPhase1({ kind: "SESSION_CREATED", sessionId, title, createdAt }, "SESSION_CREATED", "New ideation session ready", (current) => ({ ...current, activeSessionId: sessionId, sessionTitle: title, sessionCreatedAt: createdAt, turns: [], workspace: "", summary: "", sessions: [...current.sessions.filter((session) => session.id !== current.activeSessionId), snapshotSession(current)] })); setMessage(""); };
  const switchSession = (id: string) => { if (id === project.activeSessionId) return; const target = project.sessions.find((session) => session.id === id); if (!target) return; const activatedAt = now(); commitPhase1({ kind: "SESSION_ACTIVATED", sessionId: id, activatedAt }, "SESSION_ACTIVATED", `Opened “${target.title}”`, (current) => ({ ...current, activeSessionId: target.id, sessionTitle: target.title, sessionCreatedAt: target.createdAt, persona: target.persona, challenge: target.challenge ?? current.challenge, genre: target.genre, lore: target.lore, turns: target.turns, workspace: target.workspace, summary: target.summary, sessions: [...current.sessions.filter((session) => session.id !== target.id && session.id !== current.activeSessionId), snapshotSession(current)] })); setMessage(""); requestAnimationFrame(() => conversationEnd.current?.scrollIntoView()); };
  const appendToIdeas = (turn: Turn) => commitPhase1({ kind: "DRAFTING_PAPER_TURN_APPENDED", sessionId: project.activeSessionId, turnId: turn.id }, "TURN_APPENDED_TO_DRAFTING_PAPER", `Appended ${turn.speaker === "user" ? "your" : "Thinkloom's"} message to the drafting paper`, (current) => ({ ...current, workspace: `${current.workspace.trimEnd()}${current.workspace.trim() ? "\n\n" : ""}${turn.text}` }));
  const summarizeDraft = async () => { if (!project.workspace.trim() || busy) return; setBusy(true); setNotice("Distilling the drafting paper…"); const clientActionId = uid("action"); const invocationId = uid("invocation"); try { const generated = await invokeNative<string>("generate_text", { profile: project.provider, promptVariables: { action: "distill", relation: "synthesize", context: project.workspace }, cloudApproved: project.cloudApproved, purpose: "distillation", clientActionId, invocationId, sessionId: project.activeSessionId }); if (!generated?.trim()) throw new Error("The provider returned an empty response."); const summary = generated.trim(); await commitPhase1({ kind: "DISTILLATION_DISPOSED", invocationId, disposition: "accepted", summary, decidedAt: now() }, "DISTILLATION_DISPOSED", "Accepted token-efficient summary", (current) => ({ ...current, summary }), "assistant"); } catch (error) { setNotice(`Summary could not be generated: ${nativeError(error)} Check Settings → Model provider, then retry.`); } finally { setBusy(false); } };
  const exportSession = (format: "markdown" | "text") => { const heading = format === "markdown" ? "#" : ""; const section = (title: string) => `${heading ? `${heading}${heading} ` : ""}${title}`; const transcript = project.turns.map((turn) => `${turn.speaker === "user" ? "Writer" : "Thinkloom"} · ${fmt(turn.createdAt)}\n${turn.text}`).join("\n\n"); const contents = `${heading ? `${heading} ` : ""}${project.sessionTitle}\n\n${section("Session metadata")}\nCreated: ${new Date(project.sessionCreatedAt).toLocaleString()}\nPersona: ${project.persona}\nGenre: ${project.genre}\nProvider: ${project.provider.name} (${project.provider.mode})\n\n${section("Lore & context")}\n${project.lore || "None provided."}\n\n${section("Transcript")}\n${transcript || "No conversation yet."}\n\n${section("Drafting paper")}\n${project.workspace || "No ideas gathered yet."}\n\n${section("Final summary")}\n${project.summary || "No summary generated yet."}\n`; const safeName = project.sessionTitle.replace(/[^a-z0-9]+/gi, "-").replace(/^-|-$/g, "").toLowerCase() || "thinkloom-session"; download(`${safeName}.${format === "markdown" ? "md" : "txt"}`, contents, format === "markdown" ? "text/markdown" : "text/plain"); setNotice(`Exported “${project.sessionTitle}” as ${format === "markdown" ? "Markdown" : "plain text"}.`); };

  const sendMessage = async () => {
    const text = message.trim(); if (!text || busy) return; setMessage(""); setBusy(true);
    const inputMode = messageInputMode; setMessageInputMode("typed");
    const userTurn: Turn = { id: uid("turn"), speaker: "user", text, createdAt: now(), inputMode };
    const humanCommitted = await commitPhase1({ kind: "HUMAN_TURN_CREATED", sessionId: project.activeSessionId, turn: userTurn }, inputMode === "voice_transcription" ? "VOICE_TRANSCRIPTION_COMMITTED" : "HUMAN_TURN_CREATED", "Saved your conversation turn", (current) => ({ ...current, turns: [...current.turns, userTurn] }));
    if (!humanCommitted) { setBusy(false); return; }
    const conversation = [...project.turns, userTurn].slice(-10).map((turn) => `${turn.speaker === "user" ? "Writer" : "Thinkloom"}: ${turn.text}`).join("\n");
    const clientActionId = uid("action"); const invocationId = uid("invocation");
    let reply: string;
    try {
      const generated = await invokeNative<string>("generate_text", { profile: project.provider, promptVariables: { challenge: project.challenge, persona_instruction: personaInstructions[project.persona], genre_instruction: genreInstructions[project.genre] ?? genreInstructions.Fiction, lore_context: project.lore.trim() || "No additional lore or context was provided.", web_search_instruction: "When the writer asks to verify facts, search the internet, or check current data, use an available web search tool and summarize findings naturally. Clearly say when the active provider cannot search the web.", context: conversation }, cloudApproved: project.cloudApproved, purpose: "conversation", clientActionId, invocationId, sessionId: project.activeSessionId });
      if (!generated?.trim()) throw new Error("The provider returned an empty response.");
      reply = generated.trim();
      setProject((current) => ({ ...current, provider: { ...current.provider, connected: true } }));
    } catch (error) {
      setProject((current) => ({ ...current, provider: { ...current.provider, connected: false } }));
      setNotice(`${project.provider.name} did not reply: ${nativeError(error)} Check Settings → Model provider, then retry.`);
      setBusy(false); return;
    }
    const assistantTurn: Turn = { id: uid("turn"), speaker: "assistant", text: reply, createdAt: now(), inputMode: "typed", invocationId };
    await commitPhase1({ kind: "ASSISTANT_TURN_CREATED", sessionId: project.activeSessionId, turn: assistantTurn }, "ASSISTANT_RESPONSE_RETAINED", `Retained response generated by ${project.provider.name}`, (current) => ({ ...current, turns: [...current.turns, assistantTurn] }), "assistant");
    if (project.spokenReplies && "speechSynthesis" in window) { const utterance = new SpeechSynthesisUtterance(reply); const hints = voiceHints[project.persona]; utterance.voice = speechSynthesis.getVoices().find((voice) => hints.some((hint) => voice.name.includes(hint))) ?? null; speechSynthesis.speak(utterance); } setBusy(false); requestAnimationFrame(() => conversationEnd.current?.scrollIntoView({ behavior: "smooth" }));
  };
  const updateIdea = (id: string, patch: Partial<Idea>, summary = "Updated idea") => { const changedAt = now(); const existing = project.ideas.find((idea) => idea.id === id); if (!existing) return; const revised = phase1Idea({ ...existing, ...patch, revisionNumber: (existing.revisionNumber ?? 1) + 1 }, changedAt); void commitPhase1({ kind: "IDEAS_CHANGED", ideas: [revised], createdIds: [] }, "IDEAS_REVISED", summary, (current) => ({ ...current, ideas: current.ideas.map((idea) => idea.id === id ? revised : idea) })); };
  const mergeSelected = () => { if (selectedIdeas.length < 2) { setNotice("Select at least two ideas to merge."); return; } const changedAt = now(); const merged: Idea = phase1Idea({ id: uid("idea"), title: selectedIdeas.map((idea) => idea.title).join(" + ").slice(0, 70), summary: selectedIdeas.map((idea) => idea.summary).join(" "), detail: "A synthesis retaining both parent ideas.", status: "accepted", sourceTurnIds: [...new Set(selectedIdeas.flatMap((idea) => idea.sourceTurnIds))], parentIdeaIds: selectedIdeas.map((idea) => idea.id), tags: [...new Set(selectedIdeas.flatMap((idea) => idea.tags))], pinned: true, selected: true, createdBy: "mixed" }, changedAt); const revisedParents = selectedIdeas.map((idea) => phase1Idea({ ...idea, selected: false, revisionNumber: (idea.revisionNumber ?? 1) + 1 }, changedAt)); void commitPhase1({ kind: "IDEAS_CHANGED", ideas: [...revisedParents, merged], createdIds: [merged.id] }, "IDEAS_CREATED", `Merged ${selectedIdeas.length} ideas`, (current) => ({ ...current, ideas: [...current.ideas.map((idea) => revisedParents.find((parent) => parent.id === idea.id) ?? idea), merged] })); };

  const startGeneration = async (action = "draft") => {
    if (busy) return;
    if (action === "draft" && !selectedIdeas.length) { setNotice("Select at least one accepted idea first."); return; }
    await flushComposition("ai_operation");
    setBusy(true);
    const id = uid("generation");
    const source = selectedIdeas.map((idea) => idea.summary).join(" ") || "the current passage";
    const prompt = action;
    mutate("GENERATION_REQUESTED", `Started ${action.toLowerCase()} preview`, (current) => ({ ...current, generation: { ...current.generation, id, state: "streaming", text: "", prompt } }));
    try {
      const nativeText = await invokeNative<string>("generate_text", {
        profile: project.provider,
        promptVariables: { action, relation: project.generation.relation, context: source },
        cloudApproved: project.cloudApproved,
        purpose: "drafting",
        clientActionId: uid("action"),
        invocationId: id,
        sessionId: project.activeSessionId,
      });
      if (!nativeText?.trim()) throw new Error("The provider returned an empty response.");
      mutate("GENERATION_COMPLETED", "Preview ready — your manuscript is unchanged", (current) => ({ ...current, generation: { ...current.generation, id, state: "staged", text: nativeText.trim(), prompt } }), "assistant");
    } catch (error) {
      mutate("GENERATION_FAILED", "Provider request failed; your request is preserved for retry", (current) => ({ ...current, generation: { ...current.generation, id, state: "failed", text: "", prompt } }), "system");
      setNotice(`Provider request failed: ${nativeError(error)}`);
    } finally {
      setBusy(false);
    }
  };

  const acceptGeneration = (mode: "cursor" | "replace" | "append" | "section", partial = false) => {
    const full = project.generation.text; if (!full) return;
    const sentences = full.match(/.*?(?:[.!?](?=\s|$)|$)(?:\s+|$)/gs)?.filter(Boolean) ?? [full];
    const accepted = partial ? sentences.slice(0, 2).join("").trimEnd() : full;
    if (!accepted) return;
    const acceptedEnd = scalarLength(accepted); const responseEnd = scalarLength(full);
    const signal: CompositionSignal = { boundary: "ai_operation", origin: "accepted_ai_output", operationKindHint: "ai_acceptance", aiAcceptance: { invocationId: project.generation.id, responseText: full, acceptedRanges: [{ start: 0, end: acceptedEnd }], rejectedRanges: acceptedEnd < responseEnd ? [{ start: acceptedEnd, end: responseEnd }] : [], partial: acceptedEnd < responseEnd } };
    if (mode === "replace" || mode === "cursor") editor.current?.insert(accepted, mode === "replace", signal);
    else { const manuscript = mode === "section" ? `${project.manuscript.trimEnd()}\n\n## New section\n\n${accepted}\n` : `${project.manuscript.trimEnd()}\n\n${accepted}\n`; handleManuscriptChange(manuscript, signal); }
    mutate(partial ? "GENERATION_PARTIALLY_ACCEPTED" : "GENERATION_ACCEPTED", `${partial ? "Partially accepted" : "Accepted"} generated text`, (current) => ({ ...current, generation: { ...current.generation, state: "accepted" } }), "assistant");
  };
  const discardGeneration = () => mutate("GENERATION_REJECTED", "Discarded generated preview", (current) => ({ ...current, generation: { ...current.generation, state: "rejected", text: "" } }));
  const saveCheckpoint = async (name = `Version ${project.checkpoints.length + 1}`) => { await flushComposition("checkpoint"); const manuscript = latestManuscript.current; const point: Checkpoint = { id: uid("version"), name, at: now(), manuscript, words: countWords(manuscript) }; mutate("CHECKPOINT_CREATED", `Saved version “${name}”`, (current) => ({ ...current, checkpoints: [...current.checkpoints, point] })); await invokeNative("create_checkpoint", { name }); };
  const restoreCheckpoint = async (point: Checkpoint) => { const restored = await commitComposition(point.manuscript, { boundary: "restoration", origin: "system_restoration", operationKindHint: "restoration" }, `Recorded restoration of “${point.name}”`); if (!restored) return; latestManuscript.current = point.manuscript; mutate("VERSION_RESTORED", `Restored “${point.name}”`, (current) => ({ ...current, manuscript: point.manuscript })); };
  const finalize = async () => { try { await flushComposition("checkpoint"); const report = await invokeNative<NativeVerificationReport>("verify_provenance"); if (!report) { setNotice("Release finalization requires the installed native verifier."); return; } if (report.status !== "VERIFIED" && report.status !== "VERIFIED_WITH_WARNINGS") { setNotice(`Release blocked by native verification status ${report.status}. Resolve the CPL findings and verify again.`); return; } const manuscript = latestManuscript.current; const point: Checkpoint = { id: uid("release"), name: `Release ${project.checkpoints.filter((item) => item.name.startsWith("Release")).length + 1}`, at: now(), manuscript, words: countWords(manuscript) }; await invokeNative("finalize_release"); mutate("RELEASE_FINALIZED", `Finalized ${point.name}`, (current) => ({ ...current, finalized: true, checkpoints: [...current.checkpoints, point] })); } catch (error) { setNotice(`Release could not be finalized: ${nativeError(error)}`); } };
  useEffect(() => { const explicitSave = (event: KeyboardEvent) => { if (!(event.ctrlKey || event.metaKey) || event.key.toLowerCase() !== "s") return; event.preventDefault(); void flushComposition("explicit_save").then((saved) => { if (saved) setNotice("Manuscript composition saved."); }); }; window.addEventListener("keydown", explicitSave); return () => window.removeEventListener("keydown", explicitSave); }, [flushComposition]);
  const exportFile = async (format: "markdown" | "html" | "text") => { const native = await invokeNative<string>("export_project", { format, sanitized: true }); if (native) { setNotice(`Exported to ${native}`); return; } if (format === "markdown") download(`${project.title}.md`, project.manuscript, "text/markdown"); if (format === "text") download(`${project.title}.txt`, clean(project.manuscript)); if (format === "html") download(`${project.title}.html`, `<!doctype html><html><head><meta charset="utf-8"><title>${escapeHtml(project.title)}</title><style>body{max-width:720px;margin:64px auto;font:18px/1.7 Georgia,serif;padding:24px}</style></head><body>${toHtml(project.manuscript)}</body></html>`, "text/html"); mutate("EXPORT_CREATED", `Created ${format} export`, (current) => current); };
  const startVoice = () => { if (listening) { setListening(false); setNotice("Voice input stopped; no audio was retained."); return; } speechSynthesis?.cancel(); const host = window as unknown as { webkitSpeechRecognition?: new () => { lang: string; interimResults: boolean; start: () => void; onresult: (e: { results: ArrayLike<{ 0: { transcript: string } }> }) => void; onend: () => void; onerror: () => void } }; if (!host.webkitSpeechRecognition) { setNotice("Voice transcription is unavailable here. Typed input is ready."); return; } const recognition = new host.webkitSpeechRecognition(); recognition.lang = "en-US"; recognition.interimResults = true; recognition.onresult = (e) => { setMessage(Array.from(e.results).map((result) => result[0].transcript).join("")); setMessageInputMode("voice_transcription"); }; recognition.onend = () => { setListening(false); setNotice("Transcript ready to review. Audio was discarded."); }; recognition.onerror = () => { setListening(false); setNotice("Transcription failed. Audio was discarded; retry or type instead."); }; recognition.start(); setListening(true); setNotice("Listening… provisional transcript will appear below."); };
  const verifyChain = async () => { try { const report = await invokeNative<NativeVerificationReport>("verify_provenance"); if (!report) { setNotice("Native CPL verification is available in the installed desktop app."); return; } const verified = report.status === "VERIFIED" || report.status === "VERIFIED_WITH_WARNINGS"; const detail = report.findings.find((finding) => finding.severity === "CRITICAL" || finding.severity === "ERROR")?.message; setNotice(verified ? `Native integrity verification passed for ${report.event_count} events and ${report.record_count} record references.` : `Native verification status: ${report.status}.${detail ? ` ${detail}` : ""}`); } catch (error) { setNotice(`Native verification could not complete: ${nativeError(error)}`); } };
  const createEmptyNativeProject = async () => {
    await phase1Queue.current.catch(() => undefined);
    await compositionQueue.current.catch(() => undefined);
    const hasWork = Boolean(project.turns.length || project.ideas.length || project.manuscript.trim() || project.checkpoints.length);
    if (hasWork && !window.confirm("Start a new empty project? Your current project will remain in its existing folder, but unsaved in-app changes should be saved first.")) return;
    try {
      const folder = await invokeNative<string>("choose_project_folder");
      if (!folder) { setNotice("New project cancelled. Your current project is unchanged."); return; }
      const blank = createEmptyProject();
      const result = await invokeNative<{ path: string; manifest: { project_id: string } }>("create_project", { path: folder, title: blank.title });
      if (!result) { setNotice("Creating project folders is available in the installed desktop app."); return; }
      const next = { ...blank, id: result.manifest.project_id };
      setProject(next); setProjectPath(result.path); activeProjectPath.current = result.path; setReadOnlyProject(null); setMessage(""); setView("ideation");
      localStorage.setItem("thinkloom-project-v1", JSON.stringify(next));
      await initializeNativePhase1(next, "Initialized an empty Phase 1 workspace");
      const composition = await initializeNativeComposition(next); committedManuscript.current = composition?.manuscript ?? next.manuscript; latestManuscript.current = composition?.manuscript ?? next.manuscript;
      setNotice("New empty project created. Give it a title when you are ready.");
    } catch (error) { setNotice(`New project could not be created: ${nativeError(error)}`); }
  };
  const createNativeProject = async () => { await phase1Queue.current.catch(() => undefined); await compositionQueue.current.catch(() => undefined); try { const folder = await invokeNative<string>("choose_project_folder"); if (!folder) { setNotice("Choose a folder when you are ready to create the desktop project."); return; } const result = await invokeNative<{ path: string; manifest: { project_id: string } }>("create_project", { path: folder, title: project.title }); if (!result) { setNotice("Project folders are available in the installed desktop app."); return; } const imported = { ...project, id: result.manifest.project_id, updatedAt: now() }; setProject(imported); setProjectPath(result.path); activeProjectPath.current = result.path; setReadOnlyProject(null); await initializeNativePhase1(imported, "Initialized Phase 1 from the current workspace"); const composition = await initializeNativeComposition(imported); committedManuscript.current = composition?.manuscript ?? imported.manuscript; latestManuscript.current = composition?.manuscript ?? imported.manuscript; await refreshNativeFiles(imported); setNotice("Self-contained project created and ready."); } catch (error) { setNotice(`Project could not be created: ${nativeError(error)}`); } };
  const openNativeProject = async () => { await phase1Queue.current.catch(() => undefined); await compositionQueue.current.catch(() => undefined); try { const folder = await invokeNative<string>("choose_project_folder"); if (!folder) return; const result = await invokeNative<ProjectOpenResult>("open_project", { path: folder }); if (!result) { setNotice("Opening project folders is available in the installed desktop app."); return; } if (!result.editable) { setProjectPath(""); activeProjectPath.current = ""; setReadOnlyProject({ path: result.path, classification: result.classification, message: result.message }); setView("project"); setNotice(result.message); return; } setReadOnlyProject(null); setProjectPath(result.path); activeProjectPath.current = result.path; const phase1Projection = await invokeNative<Phase1Projection>("load_phase1_projection"); const composition = await invokeNative<CompositionProjection>("ensure_composition_projection"); if (!composition?.initialized) throw new Error("The manuscript composition projection could not be initialized."); committedManuscript.current = composition.manuscript; latestManuscript.current = composition.manuscript; setProject((current) => ({ ...(phase1Projection?.activeSessionId ? mergePhase1Projection(current, phase1Projection) : current), manuscript: composition.manuscript, updatedAt: composition.updatedAt })); setNotice(result.message); } catch (error) { setNotice(`Project could not be opened: ${nativeError(error)}`); } };  const showProjectFolder = async () => { try { const path = await invokeNative<string>("show_project_folder"); if (path) setNotice(`Project folder opened: ${path}`); } catch (error) { setNotice(`Project folder could not be shown: ${nativeError(error)}`); } };
  const archiveLegacyProject = async () => { try { const path = await invokeNative<string>("create_legacy_preservation_archive"); if (path) setNotice(`Byte-preserving legacy archive created at ${path}`); } catch (error) { setNotice(`Legacy preservation archive could not be created: ${nativeError(error)}`); } };
  const openPromptFolder = async () => { try { const path = await invokeNative<string>("open_prompt_folder"); if (path) { setPromptPath(path); setNotice("Prompt configuration folder opened."); } } catch (error) { setNotice(`Prompt configuration folder could not be opened: ${nativeError(error)}`); } };

  if (!hydrated) return <main className="loading"><div className="brand-mark">T</div><p>Gathering your threads…</p></main>;
  if (readOnlyProject) return <main className="app-shell">
    <header className="project-bar"><div className="brand"><span className="brand-mark">T</span><span>Thinkloom</span></div><div className="project-identity"><strong>Legacy preview project</strong><span>Read-only preservation mode</span></div></header>
    <div className="notice" role="status" aria-live="polite"><span>✦</span>{notice}</div>
    <section className="workspace"><section className="page-layout"><div className="page-heading"><span className="eyebrow">Preservation only</span><h1>This project predates the CPL 1.0 boundary</h1><p>{readOnlyProject.message}</p></div><div className="overview-grid"><article className="feature-card wide"><span className="card-index">Read-only</span><h2>No migration was attempted</h2><p>Thinkloom did not load this project into an editor, invoke recovery, regenerate provenance, modify Git history, or generate HARP.</p><code className="project-path">{readOnlyProject.path}</code><div className="project-actions"><button className="primary-button" onClick={() => void showProjectFolder()}>Show project folder</button>{readOnlyProject.classification === "LEGACY_PREVIEW_READ_ONLY" && <button className="secondary-button" onClick={() => void archiveLegacyProject()}>Create preservation archive</button>}<button className="secondary-button" onClick={() => void openNativeProject()}>Choose another project</button></div></article></div></section></section>
    <footer className="app-footer"><span>Thinkloom 0.5.11</span><span>Legacy source remains untouched</span><span>Not converted or CPL-conforming</span></footer>
  </main>;  return <main className="app-shell">
    <a className="skip-link" href="#workspace">Skip to workspace</a>
    <header className="project-bar"><button className="brand" onClick={() => navigate("project")} aria-label="Open project overview"><span className="brand-mark">T</span><span>Thinkloom</span></button><div className="project-identity"><strong>{project.title}</strong><span>{project.subtitle || "New project"}</span></div><button className="new-project-button" onClick={() => void createEmptyNativeProject()}>＋ New project</button><div className="project-status"><Mark tone={project.privacy === "Local" ? "good" : project.privacy === "Cloud" ? "cloud" : "warm"}><span className="status-dot" />{project.privacy}</Mark><button className="history-state" onClick={() => navigate("provenance")}>◇ History recorded</button><span className="save-state">Saved</span></div></header>
    <nav className="main-nav" aria-label="Main navigation"><div className="phase-nav">{(["ideation", "drafting", "finalization"] as Phase[]).map((phase, index) => <button key={phase} className={view === phase ? "active" : ""} onClick={() => navigate(phase)}><span>{index + 1}</span>{phase[0].toUpperCase() + phase.slice(1)}</button>)}</div><div className="utility-nav">{(["project", "history", "provenance", "export", "settings"] as View[]).map((item) => <button key={item} className={view === item ? "active" : ""} onClick={() => navigate(item)}>{item[0].toUpperCase() + item.slice(1)}</button>)}</div></nav>
    <div className="notice" role="status" aria-live="polite"><span>✦</span>{notice}</div>
    <section id="workspace" className="workspace" tabIndex={-1}>

    {view === "ideation" && <section className="ideation-layout">
      <aside className="context-panel panel" aria-label="AI context and sessions">
        <div className="panel-heading compact-heading"><div><span className="eyebrow">Context & session</span><h2>Shape the collaborator</h2></div><Mark tone="good">Local</Mark></div>
        <div className="context-scroll">
          <section className="config-section"><div className="section-label"><span>01</span><strong>Persona</strong></div><div className="persona-grid">{(["Supportive Coach", "Critical Editor", "Creative Partner"] as Persona[]).map((persona) => <button key={persona} className={project.persona === persona ? "active" : ""} onClick={() => void commitPhase1({ kind: "PERSONA_CHANGED", sessionId: project.activeSessionId, persona }, "PERSONA_CHANGED", `Changed persona to ${persona}`, (current) => ({ ...current, persona }))}><span className="persona-icon">{persona === "Supportive Coach" ? "◡" : persona === "Critical Editor" ? "◇" : "✦"}</span><span><strong>{persona}</strong><small>{persona === "Supportive Coach" ? "Encourage & expand" : persona === "Critical Editor" ? "Test & clarify" : "Imagine & surprise"}</small></span></button>)}</div><label>Challenge<select value={project.challenge} onChange={(event) => { const challenge = event.target.value as State["challenge"]; void commitPhase1({ kind: "CHALLENGE_CHANGED", sessionId: project.activeSessionId, challenge }, "CHALLENGE_CHANGED", `Changed challenge level to ${challenge}`, (current) => ({ ...current, challenge })); }}><option>Gentle</option><option>Balanced</option><option>Rigorous</option></select></label></section>
          <section className="config-section"><div className="section-label"><span>02</span><label htmlFor="genre">Genre focus</label></div><select id="genre" value={project.genre} onChange={(event) => { const genre = event.target.value; void commitPhase1({ kind: "GENRE_CHANGED", sessionId: project.activeSessionId, genre }, "GENRE_CHANGED", `Changed genre focus to ${genre}`, (current) => ({ ...current, genre })); }}>{Object.keys(genreInstructions).map((genre) => <option key={genre}>{genre}</option>)}</select></section>
          <section className="config-section lore-section"><div className="section-label"><span>03</span><label htmlFor="lore">Lore & context</label></div><textarea id="lore" value={project.lore} onChange={(event) => setProject((current) => ({ ...current, lore: event.target.value }))} onBlur={(event) => { const lore = event.currentTarget.value; void commitPhase1({ kind: "LORE_CHANGED", sessionId: project.activeSessionId, lore, inputKind: "manual" }, "LORE_CHANGED", "Revised lore and context", (current) => ({ ...current, lore })); }} onPaste={(event) => { event.preventDefault(); const retainedText = event.clipboardData.getData("text"); const start = event.currentTarget.selectionStart; const end = event.currentTarget.selectionEnd; const resultingText = `${project.lore.slice(0, start)}${retainedText}${project.lore.slice(end)}`; void commitPhase1({ kind: "EXTERNAL_CONTENT_DECLARED", sourceId: uid("source"), sourceKind: "paste", target: "lore", retainedText, resultingText, declaration: "Writer-declared pasted lore/context" }, "PASTED_CONTENT_DECLARED", "Declared pasted lore and context", (current) => ({ ...current, lore: resultingText })); }} placeholder="Paste character sheets, world rules, research notes, or earlier story context…" /><small>{countWords(project.lore)} words · included in the AI instruction</small></section>
          <section className="sessions-section"><div className="session-heading"><div className="section-label"><span>04</span><strong>Journals</strong></div><button onClick={startNewSession}>＋ New</button></div><button className="session-row active" aria-current="page"><span><strong>{project.sessionTitle}</strong><small>Current · {project.turns.length} messages</small></span><i>●</i></button>{[...project.sessions].sort((a, b) => b.updatedAt.localeCompare(a.updatedAt)).map((session) => <button className="session-row" key={session.id} onClick={() => switchSession(session.id)}><span><strong>{session.title}</strong><small>{fmt(session.updatedAt)} · {session.turns.length} messages</small></span><i>›</i></button>)}</section>
        </div>
        <div className="session-export"><span>Export this session</span><div><button onClick={() => exportSession("markdown")}>.md</button><button onClick={() => exportSession("text")}>.txt</button></div></div>
      </aside>

      <section className="conversation-panel panel" aria-label="Live conversation">
        <div className="panel-heading conversation-heading"><div><span className="eyebrow">Live transcript</span><input className="session-title-input" aria-label="Session title" value={project.sessionTitle} onChange={(event) => setProject((current) => ({ ...current, sessionTitle: event.target.value }))} onBlur={(event) => { const title = event.currentTarget.value; void commitPhase1({ kind: "SESSION_TITLE_REVISED", sessionId: project.activeSessionId, title, revisedAt: now() }, "SESSION_REVISED", "Revised ideation session title", (current) => ({ ...current, sessionTitle: title })); }} /></div><div className="live-indicator"><span />Ready</div></div>
        <div className="conversation-stream"><div className="date-divider"><span>{new Date(project.sessionCreatedAt).toLocaleDateString(undefined, { weekday: "long", month: "long", day: "numeric" })}</span></div>{project.turns.length === 0 && <div className="conversation-empty"><span>✦</span><h2>Start with the thought you can’t shake.</h2><p>{project.persona} is ready to explore it with you.</p></div>}{project.turns.map((turn) => <article key={turn.id} className={`turn ${turn.speaker}`}><div className="turn-author"><span className="speaker-mark">{turn.speaker === "user" ? "Y" : "T"}</span>{turn.speaker === "user" ? "You" : "Thinkloom"}<time>{fmt(turn.createdAt)}</time></div><p>{turn.text}</p><button className="append-action" onClick={() => appendToIdeas(turn)}>＋ Append to ideas</button></article>)}{busy && <article className="turn assistant thinking"><div className="turn-author"><span className="speaker-mark">T</span>Thinkloom</div><p><span /><span /><span /></p></article>}<div ref={conversationEnd} /></div>
        <div className={`composer ${listening ? "listening" : ""}`}>{listening && <div className="voice-state"><span className="pulse" />Listening — audio stays in memory and will be discarded</div>}<label htmlFor="message" className="sr-only">Continue the conversation</label><textarea id="message" value={message} onChange={(event) => setMessage(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter" && !event.shiftKey) { event.preventDefault(); void sendMessage(); } }} placeholder="Speak or type the next thread…" rows={3} /><div className="composer-actions"><div><button className={`record-button ${listening ? "recording" : ""}`} onClick={startVoice} aria-label={listening ? "Stop voice input" : "Start voice input"}><span>{listening ? "■" : "●"}</span>{listening ? "Stop" : "Talk"}</button><span className="privacy-note">No audio retained</span></div><button className="send-button" onClick={() => void sendMessage()} disabled={!message.trim() || busy}>Send <span>↗</span></button></div></div>
      </section>

      <aside className="curation-panel panel" aria-label="Idea curation and distillation">
        <div className="panel-heading compact-heading"><div><span className="eyebrow">Curation & distillation</span><h2>Gather what matters</h2></div><Mark>{countWords(project.workspace)} words</Mark></div>
        <section className="draft-paper"><div className="paper-heading"><div><span className="step-number">1</span><div><strong>Drafting paper</strong><small>Editable scratchpad</small></div></div><button onClick={() => { if (window.confirm("Clear the drafting paper and its summary?")) void commitPhase1({ kind: "DRAFTING_PAPER_REVISED", sessionId: project.activeSessionId, text: "", inputKind: "manual", clearSummary: true }, "DRAFTING_PAPER_REVISED", "Cleared the drafting paper and summary", (current) => ({ ...current, workspace: "", summary: "" })); }} disabled={!project.workspace}>Clear</button></div><textarea value={project.workspace} onChange={(event) => setProject((current) => ({ ...current, workspace: event.target.value }))} onBlur={(event) => { const text = event.currentTarget.value; void commitPhase1({ kind: "DRAFTING_PAPER_REVISED", sessionId: project.activeSessionId, text, inputKind: "manual", clearSummary: false }, "DRAFTING_PAPER_REVISED", "Saved manual drafting-paper changes", (current) => ({ ...current, workspace: text })); }} onPaste={(event) => { event.preventDefault(); const retainedText = event.clipboardData.getData("text"); const start = event.currentTarget.selectionStart; const end = event.currentTarget.selectionEnd; const resultingText = `${project.workspace.slice(0, start)}${retainedText}${project.workspace.slice(end)}`; void commitPhase1({ kind: "EXTERNAL_CONTENT_DECLARED", sourceId: uid("source"), sourceKind: "paste", target: "drafting_paper", retainedText, resultingText, declaration: "Writer-declared pasted drafting material" }, "PASTED_CONTENT_DECLARED", "Declared pasted drafting material", (current) => ({ ...current, workspace: resultingText })); }} placeholder="Append transcript moments or write freely here. This is your raw material…" aria-label="Ideas and drafting workspace" /></section>
        <div className="distill-control"><div><span className="step-number">2</span><div><strong>Distill the draft</strong><small>Remove chatter. Keep decisions.</small></div></div><button className="distill-button" onClick={() => void summarizeDraft()} disabled={!project.workspace.trim() || busy}>{busy ? "Distilling…" : "Summarize draft"}<span>✦</span></button></div>
        <section className={`summary-paper ${project.summary ? "has-summary" : ""}`}><div className="paper-heading"><div><span className="step-number">3</span><div><strong>Final summary</strong><small>Phase 2 ready</small></div></div>{project.summary && <button onClick={() => void navigator.clipboard.writeText(project.summary).then(() => setNotice("Summary copied to clipboard.")).catch(() => setNotice("Clipboard access was unavailable. Select the summary text to copy it."))}>Copy</button>}</div>{project.summary ? <p>{project.summary}</p> : <div className="summary-empty"><span>◇</span><p>Your token-efficient summary will appear here, ready for the story builder.</p></div>}</section>
      </aside>
    </section>}
    {view === "drafting" && <section className="drafting-layout">
      <aside id="ideas-panel" className="draft-ideas panel" tabIndex={-1} aria-label="Selected ideas"><div className="panel-heading"><div><span className="eyebrow">Drafting set</span><h2>Selected threads</h2></div></div><div className="relation"><label htmlFor="relation">Relation</label><select id="relation" value={project.generation.relation} onChange={(event) => setProject((current) => ({ ...current, generation: { ...current.generation, relation: event.target.value } }))}><option>synthesize</option><option>compare</option><option>contrast</option><option>sequence</option><option>support with evidence</option><option>separate into sections</option></select></div>{draftingIdeas.map((idea) => <label className={`draft-idea ${idea.selected ? "selected" : ""}`} key={idea.id}><input type="checkbox" checked={idea.selected} onChange={() => updateIdea(idea.id, { selected: !idea.selected }, `${idea.selected ? "Deselected" : "Selected"} “${idea.title}”`)} /><span><strong>{idea.title}</strong><small>{idea.summary}</small></span></label>)}<button className="secondary-button full" onClick={mergeSelected}>Merge selected</button><p className="shortcut">Focus: Alt+1</p></aside>
      <section className="manuscript-panel panel" aria-label="Manuscript editor"><div className="editor-toolbar"><div><span className="eyebrow">Manuscript</span><h1>{project.title}</h1></div><div><button onClick={() => { void saveCheckpoint(); }}>Save version</button><span>{countWords(project.manuscript).toLocaleString()} words</span></div></div><StructuredEditor ref={editor} value={project.manuscript} onChange={handleManuscriptChange} onCommit={(boundary) => { void flushComposition(boundary); }} /><div className="editor-footer"><span>Markdown structure · Autosaved locally</span><span>Focus: Alt+2</span></div></section>
      <aside id="assistant-panel" className="draft-assistant panel" tabIndex={-1} aria-label="Generation assistant"><div className="panel-heading"><div><span className="eyebrow">Writing room</span><h2>Shape a passage</h2></div></div><p className="assistant-context">Using {selectedIdeas.length} selected idea{selectedIdeas.length === 1 ? "" : "s"} and your project voice. Your manuscript will not change until you choose.</p><button className="primary-button full" onClick={() => void startGeneration()} disabled={busy || !selectedIdeas.length}>{busy ? "Gathering threads…" : "Draft a passage"}</button>{project.generation.state === "staged" ? <div className="generation-preview"><div className="preview-label"><span>Preview</span><Mark tone="warm">AI draft · not inserted</Mark></div><p>{project.generation.text}</p><div className="preview-actions"><button className="primary-button" onClick={() => acceptGeneration("cursor")}>Insert at cursor</button><button onClick={() => acceptGeneration("replace")}>Replace selection</button><button onClick={() => acceptGeneration("append")}>Append</button><button onClick={() => acceptGeneration("section")}>New section</button><button onClick={() => acceptGeneration("cursor", true)}>Insert first 2 sentences</button><button onClick={() => void startGeneration()}>Regenerate</button><button className="danger-text" onClick={discardGeneration}>Discard</button></div></div> : <Empty eyebrow="Preview first" title="Nothing enters unseen" body="Choose your ideas, set their relationship, and generate a passage to review here." />}<p className="shortcut">Focus: Alt+3</p></aside>
    </section>}
    {view === "finalization" && <section className="finalization-layout">
      <aside className="editorial-rail panel" aria-label="Editorial actions"><span className="eyebrow">Editorial pass</span><h2>Refine with intent</h2><p>Select text in the manuscript, then preview an action.</p><div className="action-stack">{["Clarity", "Rewrite", "Shorten", "Expand", "Transition", "Repetition", "Consistency", "Tone", "User voice", "Proofread"].map((action) => <button key={action} onClick={() => void startGeneration(action)}>{action}<span>→</span></button>)}</div></aside>
      <section className="manuscript-panel panel final-manuscript"><div className="editor-toolbar"><div><span className="eyebrow">Release manuscript</span><h1>{project.title}</h1></div><Mark tone={project.finalized ? "good" : "warm"}>{project.finalized ? "Release ready" : "Working version"}</Mark></div><StructuredEditor ref={editor} value={project.manuscript} onChange={handleManuscriptChange} onCommit={(boundary) => { void flushComposition(boundary); }} label="Final manuscript editor" /><div className="editor-footer"><span>{countWords(project.manuscript)} words</span><span>{project.checkpoints.length} saved versions</span></div></section>
      <aside className="release-panel panel"><span className="eyebrow">Release desk</span><h2>Prepare to publish</h2><div className="release-check"><span>·</span><div><strong>Native verification required</strong><small>{project.activity.length} recent actions shown; finalization re-verifies the complete CPL</small></div></div><div className="release-check"><span>✓</span><div><strong>Manuscript has a title</strong><small>{countWords(project.manuscript)} words ready</small></div></div><div className="release-check"><span>{project.checkpoints.length ? "✓" : "·"}</span><div><strong>Version saved</strong><small>{project.checkpoints.at(-1)?.name ?? "Save a version first"}</small></div></div>{project.generation.state === "staged" && <div className="generation-preview compact"><div className="preview-label"><span>Editorial preview</span><Mark tone="warm">Not applied</Mark></div><p>{project.generation.text}</p><div className="preview-actions"><button className="primary-button" onClick={() => acceptGeneration("replace")}>Apply to selection</button><button onClick={discardGeneration}>Discard</button></div></div>}<button className="secondary-button full" onClick={() => saveCheckpoint("Pre-release review")}>Save review version</button><button className="release-button" onClick={() => { void finalize(); }}>Finalize release ↗</button><p className="fine-print">Finalizing creates a named, restorable release. You can continue writing afterward.</p></aside>
    </section>}

    {view === "project" && <section className="page-layout"><div className="page-heading"><span className="eyebrow">One publication, one place</span><h1>Project overview</h1><p>Everything needed to restore this publication travels with it.</p></div><div className="overview-grid"><article className="feature-card wide"><span className="card-index">01</span><h2>{project.title}</h2><input aria-label="Project title" placeholder="Name your project" value={project.title} onChange={(event) => setProject((current) => ({ ...current, title: event.target.value }))} /><textarea aria-label="Project description" placeholder="What are you exploring?" value={project.subtitle} onChange={(event) => setProject((current) => ({ ...current, subtitle: event.target.value }))} /><div className="stat-row"><div><strong>{countWords(project.manuscript)}</strong><span>words</span></div><div><strong>{project.ideas.filter((idea) => idea.status === "accepted").length}</strong><span>accepted ideas</span></div><div><strong>{project.activity.length}</strong><span>recent actions</span></div></div></article><article className="feature-card"><span className="card-index">02</span><h2>Project health</h2><p>Writing, ideas, and creative history are autosaved. Canonical files are refreshed by the desktop app.</p><div className="project-actions"><button className="primary-button" onClick={() => void createEmptyNativeProject()}>New empty project</button><button className="secondary-button" onClick={() => void createNativeProject()}>Save current to folder</button><button className="secondary-button" onClick={() => void openNativeProject()}>Open existing</button><button className="secondary-button" onClick={verifyChain}>Verify history</button>{projectPath && <button className="secondary-button" onClick={() => void showProjectFolder()}>Show project folder</button>}</div>{projectPath && <small className="project-path">{projectPath}</small>}</article><article className="feature-card"><span className="card-index">03</span><h2>Recovery</h2><p>Rotating snapshots keep the last seven valid project states outside the active folder.</p><button className="secondary-button" onClick={() => setNotice("Latest recovery point verified.")}>Check recovery point</button></article></div></section>}

    {view === "history" && <section className="page-layout"><div className="page-heading"><span className="eyebrow">Restorable moments</span><h1>Version history</h1><p>Compare or return to meaningful stages without technical terminology.</p></div><div className="history-layout"><div className="version-list panel">{[...project.checkpoints].reverse().map((point, index) => <article className="version-row" key={point.id}><div className="version-symbol">{index === 0 ? "◆" : "◇"}</div><div><strong>{point.name}</strong><span>{fmt(point.at)} · {point.words} words</span></div><div><button onClick={() => setNotice(`Compared with “${point.name}”: ${countWords(project.manuscript) - point.words >= 0 ? "+" : ""}${countWords(project.manuscript) - point.words} words.`)}>Compare</button><button onClick={() => { void restoreCheckpoint(point); }}>Restore</button></div></article>)}</div><aside className="panel history-aside"><span className="eyebrow">Current work</span><h2>{countWords(project.manuscript)} words</h2><p>{project.checkpoints.length ? `${countWords(project.manuscript) - project.checkpoints.at(-1)!.words >= 0 ? "+" : ""}${countWords(project.manuscript) - project.checkpoints.at(-1)!.words} words since ${project.checkpoints.at(-1)!.name}.` : "Save your first version when the draft reaches a meaningful moment."}</p><button className="primary-button full" onClick={() => { void saveCheckpoint(); }}>Save current version</button></aside></div></section>}

    {view === "provenance" && <ProvenanceWorkspace onNotice={setNotice} />}
    {view === "export" && <section className="page-layout"><div className="page-heading"><span className="eyebrow">Take your work with you</span><h1>Publish, preserve, or share</h1><p>Exports are created from the current release manuscript and checked before completion.</p></div><div className="export-grid"><article className="export-card"><span className="export-type">Publication</span><h2>Reading files</h2><p>Clean files for editors, websites, and your own archive.</p><div className="export-buttons"><button onClick={() => void exportFile("markdown")}>Markdown <span>.md</span></button><button onClick={() => void exportFile("html")}>Web page <span>.html</span></button><button onClick={() => void exportFile("text")}>Plain text <span>.txt</span></button><button onClick={() => setNotice("PDF export is available in the installed desktop app.")}>Print-ready <span>.pdf</span></button></div></article><article className="export-card featured"><span className="export-type">Preservation</span><h2>Project backup</h2><p>A complete, restorable package with manuscript, ideas, history, and recovery data.</p><button className="primary-button" onClick={() => void invokeNative<string>("create_backup").then((path) => setNotice(path ? `Backup created at ${path}` : "Project Backup ZIP is available in the desktop app."))}>Create project backup</button></article><HarpExportPanel onNotice={setNotice} /></div></section>}

    {view === "settings" && <section className="page-layout"><div className="page-heading"><span className="eyebrow">Private by design</span><h1>Settings</h1><p>Choose where thinking happens and how Thinkloom supports the work.</p></div><div className="settings-grid">
      <section className="settings-section panel"><div className="settings-heading"><span className="settings-number">01</span><div><h2>Model provider</h2><p>Your active writing assistant.</p></div></div><label>Provider<select value={project.provider.kind} onChange={(event) => { const kind = event.target.value as Provider["kind"]; const profile: Provider = kind === "ollama" ? { kind, name: "Ollama", endpoint: "http://127.0.0.1:11434", model: "llama3.2", mode: "local", connected: false } : kind === "openai" ? { kind, name: "OpenAI", endpoint: "https://api.openai.com/v1", model: "gpt-4.1-mini", mode: "cloud", connected: false } : { kind, name: "Compatible endpoint", endpoint: "http://127.0.0.1:1234/v1", model: "local-model", mode: "local", connected: false }; void commitPhase1({ kind: "PROVIDER_CONTEXT_CHANGED", provider: profile }, "PROVIDER_CONTEXT_CHANGED", `Changed provider to ${profile.name}`, (current) => ({ ...current, provider: profile, privacy: profile.mode === "local" ? "Local" : "Cloud" })); }}><option value="ollama">Ollama · Local</option><option value="openai">OpenAI · Cloud</option><option value="compatible">OpenAI-compatible</option></select></label><label>Endpoint<input value={project.provider.endpoint} onChange={(event) => setProject((current) => ({ ...current, provider: { ...current.provider, endpoint: event.target.value } }))} onBlur={(event) => { const provider = { ...project.provider, endpoint: event.currentTarget.value, connected: false }; void commitPhase1({ kind: "PROVIDER_CONTEXT_CHANGED", provider }, "PROVIDER_CONTEXT_CHANGED", "Revised provider endpoint", (current) => ({ ...current, provider })); }} /></label><label>Model<input value={project.provider.model} onChange={(event) => setProject((current) => ({ ...current, provider: { ...current.provider, model: event.target.value } }))} onBlur={(event) => { const provider = { ...project.provider, model: event.currentTarget.value, connected: false }; void commitPhase1({ kind: "PROVIDER_CONTEXT_CHANGED", provider }, "PROVIDER_CONTEXT_CHANGED", "Revised provider model", (current) => ({ ...current, provider })); }} /></label>{project.provider.kind !== "ollama" && <label>Credential<input type="password" autoComplete="new-password" value={credential} onChange={(event) => setCredential(event.target.value)} placeholder="Stored only in your system vault" /><button className="secondary-button" type="button" onClick={() => void invokeNative("store_provider_secret", { profileId: project.provider.kind, secret: credential }).then(() => { setCredential(""); setNotice("Credential saved in the operating-system vault."); }).catch((error) => setNotice(`Credential could not be saved: ${String(error)}`))}>Save securely</button></label>}{project.provider.mode === "cloud" && !project.cloudApproved && <div className="cloud-warning"><strong>Cloud approval required</strong><p>Your first cloud request sends only relevant context shown in preview.</p><button onClick={() => void commitPhase1({ kind: "CLOUD_APPROVAL_CHANGED", approved: true }, "CLOUD_APPROVAL_CHANGED", "Approved cloud processing for this project", (current) => ({ ...current, cloudApproved: true }))}>Approve for this project</button></div>}<button className="secondary-button" onClick={() => { const clientActionId = uid("action"); const invocationId = uid("invocation"); void invokeNative<{ ok: boolean; message: string }>("test_provider", { profile: project.provider, clientActionId, invocationId, sessionId: project.activeSessionId }).then((result) => { const provider = { ...project.provider, connected: Boolean(result?.ok) }; void commitPhase1({ kind: "PROVIDER_CONTEXT_CHANGED", provider }, "PROVIDER_CONTEXT_CHANGED", result?.message ?? "Provider test completed", (current) => ({ ...current, provider })); }).catch((error) => setNotice(`Provider test failed: ${nativeError(error)}`)); }}>Test connection</button></section>
      <section className="settings-section panel"><div className="settings-heading"><span className="settings-number">02</span><div><h2>Voice & listening</h2><p>Audio is processed in memory and never retained.</p></div></div><label className="toggle-row"><input type="checkbox" checked={project.spokenReplies} onChange={(event) => setProject((current) => ({ ...current, spokenReplies: event.target.checked }))} /><span><strong>Spoken assistant replies</strong><small>Always accompanied by visible text. Off by default.</small></span></label><button className="secondary-button" onClick={startVoice}>Test microphone</button></section>
      <section className="settings-section panel wide"><div className="settings-heading"><span className="settings-number">03</span><div><h2>Your writing voice</h2><p>Inspectable traits guide suggestions without impersonating you.</p></div></div><div className="trait-list">{project.styleTraits.map((trait, index) => <label key={`${index}-${trait}`}><span className="confidence">Developing</span><input value={trait} onChange={(event) => setProject((current) => ({ ...current, styleTraits: current.styleTraits.map((item, itemIndex) => itemIndex === index ? event.target.value : item) }))} /></label>)}</div><label>Habit to avoid<div className="inline-input"><input value={newHabit} onChange={(event) => setNewHabit(event.target.value)} placeholder="Add a pattern to avoid" /><button onClick={() => { if (!newHabit.trim()) return; mutate("STYLE_PROFILE_UPDATED", "Updated writing voice profile", (current) => ({ ...current, disallowedHabits: [...current.disallowedHabits, newHabit.trim()] })); setNewHabit(""); }}>Add</button></div></label><div className="tag-row">{project.disallowedHabits.map((habit) => <span key={habit}>{habit}</span>)}</div></section>
      <section className="settings-section panel wide prompt-settings"><div className="settings-heading"><span className="settings-number">04</span><div><h2>Prompt configuration</h2><p>Technical users can tune every instruction sent to the model.</p></div></div><p><strong>conversation.json</strong> affects Ideation replies. <strong>drafting.json</strong> affects passage and editorial previews. Files reload before every model request, so no restart is needed.</p><code className="prompt-path">{promptPath || "Preparing prompt files…"}</code><div className="project-actions"><button className="secondary-button" onClick={() => void openPromptFolder()}>Open prompt folder</button></div><small>README.md in this folder documents every field, variable, effect, validation rule, and reset procedure.</small></section>
    </div></section>}
    </section>
    <footer className="app-footer"><span>Thinkloom 0.5.11</span><span>Local-first · audio retention always off</span><span>CPL 1.0 project boundary</span></footer>
  </main>;
}
